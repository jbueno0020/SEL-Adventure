import SELAdventure from '@game';

// Gatsby page — renders the SEL Adventure game component.
// The game source lives at the project root as "Game v1" (the
// standalone, copy-pasteable version suitable for TPT distribution).
export default SELAdventure;
